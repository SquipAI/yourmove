---
import type { ShowcaseDemo } from "@lib/types";

interface Props {
  demo: Extract<ShowcaseDemo, { _type: "bioDemo" }>;
  class?: string;
}
const { demo, class: cls } = Astro.props;
---

<blockquote class:list={["text-sm leading-relaxed", cls]}>
  &ldquo;{demo.text}&rdquo;
</blockquote>
