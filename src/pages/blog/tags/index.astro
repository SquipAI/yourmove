---
import TagListPage from "@components/blog/pages/TagListPage.astro";
import { getAllTagsWithCount, getBlogTagsPage } from "@lib/queries";

const [tags, page] = await Promise.all([
  getAllTagsWithCount(),
  getBlogTagsPage(),
]);
---

<TagListPage {tags} {page} />
